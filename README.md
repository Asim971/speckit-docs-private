# SpecKit Documentation Mirror

This repository mirrors the `docs/` directory from the SpecKit workspace.

## Sync Workflow

- Source: SpecKit repository (local working copy)
- Destination: GitHub private repository

Run `npm run docs:sync` from the SpecKit workspace to refresh the mirror.